[project]
name = "kanjuware"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pyinstaller>=6.20.0",
    "pymem>=1.14.0",
]
